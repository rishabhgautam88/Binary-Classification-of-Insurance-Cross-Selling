# Model Training

The modeling section uses scikit-learn utilities for transformations and estimator training.

When changing the model, keep preprocessing and estimator configuration close together so the notebook remains reproducible.

