# Notebook Workflow

The analysis starts by loading the training data, checking shape and schema, reviewing summary statistics, and validating missing values.

Later sections inspect duplicates, categorical distributions, feature relationships, model training, test prediction, and report generation.

