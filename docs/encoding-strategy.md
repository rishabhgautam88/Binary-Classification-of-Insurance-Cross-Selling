# Encoding Strategy

The notebook prepares categorical and binary-like columns before model training.

Keep encoding decisions explicit because tree-based ensembles and linear models can react differently to ordinal, one-hot, and binary encodings.

