# Prediction Workflow

The prediction flow loads test data, preserves the `id` column, applies matching transformations, and prepares predicted outputs.

The same feature preparation used during training should be reused for test data to avoid schema drift.

