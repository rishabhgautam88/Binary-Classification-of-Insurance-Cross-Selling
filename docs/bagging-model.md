# Bagging Model

The notebook includes a `BaggingClassifier` experiment.

Bagging can reduce variance by training multiple base estimators on sampled versions of the data and combining their predictions.

