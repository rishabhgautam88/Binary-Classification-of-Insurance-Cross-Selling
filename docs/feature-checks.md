# Feature Checks

The notebook checks column types and converts `Region_Code` into an integer representation.

It also separates binary-like columns and object columns so categorical handling can be reviewed before modeling.

