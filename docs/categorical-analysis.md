# Categorical Analysis

Categorical features such as `Gender`, `Vehicle_Age`, and `Vehicle_Damage` are inspected with unique-value checks and cross-tab comparisons.

These checks help decide whether a feature should be encoded, grouped, or reviewed for leakage.

